meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build
  artifacts:
    "*": /

run:
  command: npm run start
  containerPort: "80"
  persistenceMount: /data
