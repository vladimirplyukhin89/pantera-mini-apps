{
  "name": "pantera-mini-apps",
  "private": true,
  "description": "Корневой манифест для деплоя Strapi на Amvera (npm из корня репозитория).",
  "scripts": {
    "postinstall": "npm install --prefix strapi-cms",
    "build": "npm run build --prefix strapi-cms",
    "start": "npm run start --prefix strapi-cms"
  }
}
