# Скопируйте в .env и подставьте значения. Файл .env не коммитится.
#
# Локально: http://localhost:1337 при запущенном strapi-cms (`npm run develop`).
# Продакшен-сборка против облака: https://<проект>.strapiapp.com

STRAPI_URL=http://localhost:1337
STRAPI_TOKEN=
